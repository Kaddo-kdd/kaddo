# Tasks: Config & State Plumbing

## Implementation Tasks

- [x] Review `.kaddo/config.yml` structure generated by `kaddo init`.
- [x] Create centralized config reader (`src/core/config.ts`).
- [x] Define config schema with Zod.
- [x] Validate config when reading; clear errors on invalid.
- [x] Apply safe defaults for missing optional fields.
- [x] Add state-aware helpers (`nextStepsForState`, `createGuidanceForState`, `describeProject`).
- [x] Update `scan` to print a state-aware next step.
- [x] Update `create` to print state-aware helper text.
- [x] Handle missing config gracefully.
- [x] Avoid breaking existing commands.

## Tests

- [x] Load valid config.
- [x] Missing config returns null.
- [x] Invalid YAML throws a clear error.
- [x] Invalid project state throws a validation error.
- [x] Backward-compatible config with missing fields uses defaults.
- [x] State-aware helper output differs by state.

## Documentation

- [x] Document `.kaddo/config.yml` and project states (new / pre-ai / legacy), team sizes, structures.
- [x] Update scan docs with state-aware behavior.

## Validation

- [x] Run tests (`pnpm test`).
- [x] Run build (`pnpm -r build`).
- [ ] Manual test with state new / pre-ai / legacy.
