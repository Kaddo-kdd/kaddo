# Tasks: Bootstrap Minimal Artifacts & Agent Groups

## Phase 1 — Consolidated bootstrap

- [ ] Add consolidated `business` and `product` templates; expand `codebase` template.
- [ ] Flag specialized templates as `advanced: true` (kept, not generated by bootstrap).
- [ ] Reduce `kaddo bootstrap` to business.md / product.md / codebase.md.
- [ ] Update bootstrap tests/fixtures. Keep green.

## Phase 2 — Agent groups

- [ ] Add `src/agents/groups.ts` (group map + recommended-by-state + selector).
- [ ] `kaddo add agents` installs the recommended set by state by default.
- [ ] Add `--all` and `--group <name>` to `kaddo add agents`.
- [ ] `kaddo understand` recommends contextual agents (state + layer + installed).
- [ ] Tests for groups, defaults, flags and understand.

## Phase 3 — Docs & examples (EN/ES)

- [ ] Manifesto: "Knowledge grows progressively".
- [ ] Bootstrap pages: business.md / product.md / codebase.md as starting point.
- [ ] Agents page: "Agent groups" + `--all` / `--group`.
- [ ] Visual Guide: progressive arrival of artifacts/agents.
- [ ] Update the `new-project` example.

## Validation

- [ ] `pnpm --filter "@kaddo/cli" test`
- [ ] `pnpm -r build`
