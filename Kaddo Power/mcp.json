{
  "$schema": "https://agent-plugins.org/schemas/1.0.0/mcp.schema.json",
  "mcpServers": {
    "kaddo": {
      "type": "stdio",
      "command": "npx",
      "args": ["-y", "@kaddo/mcp"],
      "env": {
        "KADDO_PROJECT_DIR": "${KADDO_PROJECT_DIR}"
      }
    }
  }
}
